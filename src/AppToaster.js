import { Position, Toaster } from '@blueprintjs/core'

export const AppToasterSuccess = Toaster.create({
  position: Position.TOP,
})

export const AppToasterFail = Toaster.create({
  position: Position.TOP,
})
